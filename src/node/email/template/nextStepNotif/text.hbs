{{#if existed}}
    {{#if assignedUserName}}
        {{modifiedByName}} has assigned {{stepTitle}} to {{assignedUserName}}
    {{else}}
        {{modifiedByName}} has updated {{stepTitle}}
    {{/if}}
{{else if assignedUserName}}
    {{modifiedByName}} has assigned {{stepTitle}} to {{assignedUserName}}
{{else}}
    {{modifiedByName}} has created {{stepTitle}}
{{/if}}

Opportunity Name:
{{dealName}}

Next Step Creator:
{{authorName}}

Assigned User:
{{assignedUserName}}

Description:
{{description}}


Due Date:
{{formatDate dueDate}}

Incomplete Next Steps:
{{incompleteSteps.length}}

{{>roost-link-text}}

{{> footer-text}}
