{{#if existed}}
    {{#if completedDate}}
        {{modifiedByName}} has completed {{stepTitle}}
    {{else}}
        {{modifiedByName}} has updated {{stepTitle}}
    {{/if}}
{{else if assignedUserName}}
    {{modifiedByName}} has assigned a next step to {{assignedUserName}}
{{else}}
    {{modifiedByName}} has created {{stepTitle}}
{{/if}}

Opportunity Name:
{{dealName}}

Next Step Creator:
{{authorName}}

Assigned User:
{{assignedUserName}}

Description:
{{description}}


Due Date:
{{formatDate dueDate}}

Status:
{{stepStatusLabel completedDate}}

Incomplete Next Steps:
{{incompleteSteps.length}}

{{>roost-link-text}}

{{> footer-text}}
